// metro.config.js
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

// If you use SVGs or custom asset extensions you can tweak config here.

module.exports = config;

